{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "finstack-data": {
      "command": "./engine/dist/finstack",
      "args": ["mcp-server"],
      "env": {
        "FINSTACK_HOME": "${PLUGIN_DATA}"
      }
    }
  }
}
