{
  "mcpServers": {
    "finstack-data": {
      "command": "./bin/finstack-runtime",
      "args": ["mcp-server"],
      "tool_timeout_sec": 86400,
      "env": {
        "FINSTACK_HOME": "${PLUGIN_DATA}"
      }
    }
  }
}
