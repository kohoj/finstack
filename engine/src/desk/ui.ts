// Desk ships as same-origin assets. No CDN, inline script, or remote font can
// become a quiet exception to the loopback security boundary.

export function renderDeskHtml(): string {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="referrer" content="no-referrer">
  <title>finstack — Desk</title>
  <link rel="stylesheet" href="/app.css">
</head>
<body>
  <main id="app" aria-live="polite"><div class="loading">Opening your Desk</div></main>
  <script src="/app.js" defer></script>
</body>
</html>`;
}

export function renderDeskCss(): string {
  return `
:root{color-scheme:dark;--canvas:#0a0c0d;--panel:#111516;--raised:#171d1d;--line:#293133;--soft:#202728;--text:#f2f5f2;--muted:#9aa3a1;--quiet:#66706e;--green:#7ee5a8;--amber:#f2c66d;--red:#f27f80;--blue:#91bfff;font-family:ui-sans-serif,-apple-system,BlinkMacSystemFont,"SF Pro Text","Segoe UI",sans-serif}
*{box-sizing:border-box}body{min-width:0;margin:0;background:var(--canvas);color:var(--text)}button,input,textarea{font:inherit}.loading{min-height:100vh;display:grid;place-content:center;color:var(--muted);font-size:14px}.shell{min-height:100vh;display:grid;grid-template-rows:56px minmax(0,1fr)}.tape{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:0 22px;border-bottom:1px solid var(--line);background:#0d1011}.brand{display:flex;align-items:baseline;gap:10px;min-width:0}.wordmark{font-size:13px;font-weight:760;letter-spacing:.15em;text-transform:uppercase}.section-name,.count,.status{color:var(--muted);font:11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.04em}.status{display:flex;align-items:center;gap:8px;white-space:nowrap}.dot{width:7px;height:7px;border-radius:99px;background:var(--green);box-shadow:0 0 12px rgba(126,229,168,.65)}.desk{min-height:0;display:grid;grid-template-columns:252px minmax(0,1fr) 312px}.book,.signals{overflow:auto;padding:20px 14px;background:var(--panel)}.book{border-right:1px solid var(--line)}.signals{border-left:1px solid var(--line)}.book-hint{display:none}.pane-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin:0 4px 14px}.pane-head h2,.card h2,.detail-header h2{margin:0;font-size:11px;font-weight:760;letter-spacing:.1em;text-transform:uppercase}.position{width:100%;padding:12px 9px;border:1px solid transparent;border-bottom-color:var(--soft);border-radius:8px;background:transparent;color:inherit;text-align:left;cursor:pointer}.position:hover,.position[aria-pressed="true"]{border-color:#3a4744;background:#1b2221}.position-row,.position-meta{display:flex;justify-content:space-between;align-items:baseline;gap:10px}.ticker{font-weight:720;letter-spacing:.01em}.weight,.numeric,.alloc-value,.metric-value,.ticket-value b{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-variant-numeric:tabular-nums}.weight{font-size:12px}.position-meta{margin-top:5px;color:var(--muted);font-size:11px}.stage{min-width:0;overflow:auto;padding:28px 30px 42px;background:radial-gradient(circle at 48% -25%,#19221e 0,transparent 38%),var(--canvas)}.stage-header{display:flex;align-items:flex-start;justify-content:space-between;gap:22px}.stage-header h1{max-width:700px;margin:7px 0 0;font-size:clamp(27px,3.4vw,46px);font-weight:640;letter-spacing:-.045em;line-height:1.03}.eyebrow{color:var(--quiet);font-size:10px;font-weight:760;letter-spacing:.12em;text-transform:uppercase}.subhead{max-width:700px;margin:11px 0 0;color:var(--muted);font-size:14px;line-height:1.5}.button{min-height:36px;padding:8px 11px;border:1px solid transparent;border-radius:7px;background:var(--green);color:#102117;cursor:pointer;font-size:12px;font-weight:760;white-space:nowrap}.button.ghost{border-color:#3c4845;background:#202827;color:var(--text)}.summary-grid{display:grid;grid-template-columns:minmax(0,1.5fr) repeat(3,minmax(125px,.7fr));gap:10px;margin-top:26px}.card{border:1px solid var(--line);border-radius:10px;background:linear-gradient(145deg,rgba(24,30,30,.98),rgba(14,18,19,.98))}.summary-card{min-height:132px;padding:17px}.summary-card.primary{display:flex;flex-direction:column;justify-content:space-between;border-color:#3a4945}.metric-value{margin-top:13px;font-size:30px;font-weight:680;letter-spacing:-.06em;line-height:1}.metric-label{margin-top:7px;color:var(--muted);font-size:11px;line-height:1.4}.value{margin-top:14px;font-size:clamp(34px,4.5vw,56px);font-weight:650;letter-spacing:-.065em;line-height:.9}.good{color:var(--green)}.warning{color:var(--amber)}.bad{color:var(--red)}.layout-grid{display:grid;grid-template-columns:minmax(0,1.18fr) minmax(280px,.82fr);gap:12px;margin-top:12px}.stack{display:grid;gap:12px}.card-body{padding:18px}.card-note{margin:8px 0 0;color:var(--muted);font-size:12px;line-height:1.45}.attention-list,.stress-grid{display:grid;gap:8px;margin-top:15px}.attention-item{padding:11px 12px;border-left:3px solid var(--amber);border-radius:0 7px 7px 0;background:#201d15;color:#f2dfae;font-size:12px;line-height:1.45}.attention-item.critical{border-color:var(--red);background:#261718;color:#f2b4b5}.attention-item.clear{border-color:var(--green);background:#142019;color:#b6e7c5}.allocation-list{display:grid;gap:10px;margin-top:17px}.allocation-row{display:grid;grid-template-columns:58px minmax(80px,1fr) 44px;gap:10px;align-items:center;font-size:12px}.track{height:6px;overflow:hidden;border-radius:99px;background:#28302f}.fill{height:100%;border-radius:inherit;background:linear-gradient(90deg,#61c98c,#a3e6b4)}.alloc-value{color:var(--muted);text-align:right}.stress{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:12px;border:1px solid var(--soft);border-radius:8px;background:#111718}.stress strong{display:block;font-size:12px}.stress span{display:block;margin-top:4px;color:var(--muted);font-size:11px}.stress-impact{align-self:center;color:var(--red);font:15px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:-.04em}.mirror-head{display:flex;align-items:baseline;justify-content:space-between;gap:12px}.line-chart{width:100%;height:116px;margin-top:17px;overflow:visible}.line-chart line{stroke:#2b3634;stroke-width:1}.line-chart polyline{fill:none;stroke:var(--blue);stroke-width:2.2;stroke-linejoin:round;stroke-linecap:round}.empty-chart{display:grid;min-height:116px;place-content:center;margin-top:17px;padding:16px;border:1px dashed #34403d;border-radius:7px;color:var(--muted);font-size:12px;line-height:1.45;text-align:center}.mirror-footer{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-top:14px}.mirror-footer span{color:var(--muted);font-size:11px;line-height:1.45}.integrity{padding:13px;border:1px solid var(--line);border-radius:8px;background:#151b1a}.integrity strong{display:block;font-size:13px}.integrity p,.signal p{margin:6px 0 0;color:var(--muted);font-size:12px;line-height:1.48}.section-rule{height:1px;margin:19px 0;background:var(--line)}.signal{padding:12px 4px;border-bottom:1px solid var(--soft)}.signal h3{margin:0;font-size:12px}.empty{color:var(--muted);font-size:12px;line-height:1.5}.detail-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:25px}.detail-stat{min-height:95px;padding:13px;border:1px solid var(--line);border-radius:8px;background:#121718}.detail-stat span,.ticket-value span{display:block;color:var(--muted);font-size:10px}.detail-stat b{display:block;margin-top:8px;font:16px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:-.045em}.detail-card{margin-top:12px}.detail-header{display:flex;align-items:center;justify-content:space-between;gap:16px}.source-line{margin-top:13px;color:var(--muted);font-size:12px;line-height:1.5}.mark-form{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,.9fr) auto;gap:8px;margin-top:16px}.field{display:grid;gap:5px;color:var(--muted);font-size:10px;font-weight:700;letter-spacing:.07em;text-transform:uppercase}input,textarea{width:100%;border:1px solid #46514e;border-radius:6px;outline:none;background:#0c1010;color:var(--text)}input{min-height:37px;padding:8px 9px}textarea{min-height:88px;padding:9px;resize:vertical}input:focus,textarea:focus{border-color:var(--green);box-shadow:0 0 0 3px rgba(126,229,168,.11)}.toast{position:fixed;right:20px;bottom:20px;z-index:5;max-width:380px;padding:11px 13px;border:1px solid #426653;border-radius:8px;background:#13251b;color:var(--green);box-shadow:0 14px 44px rgba(0,0,0,.32);font-size:12px}.toast.bad{border-color:#7f4143;background:#2b1718;color:#f4b0b1}.decision{position:fixed;z-index:4;inset:0;display:grid;place-items:center;padding:20px;background:rgba(0,0,0,.68);backdrop-filter:blur(7px)}.decision-card{width:min(590px,100%);max-height:calc(100vh - 40px);overflow:auto;padding:24px;border:1px solid #4b5d57;border-radius:12px;background:#121918;box-shadow:0 24px 80px rgba(0,0,0,.54)}.decision-card h2{margin:8px 0 0;font-size:21px;letter-spacing:-.025em}.decision-card>p{margin:11px 0 0;color:var(--muted);font-size:13px;line-height:1.55;white-space:pre-wrap}.ticket-values{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:17px}.ticket-value{padding:10px;border-radius:7px;background:#0d1211}.ticket-value b{display:block;margin-top:5px;font-size:13px;overflow-wrap:anywhere}.ticket-controls{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px}.ticket-risk{margin-top:10px!important;color:var(--amber)!important}.decision-actions{display:flex;justify-content:flex-end;flex-wrap:wrap;gap:8px;margin-top:20px}
@media(max-width:1120px){.desk{grid-template-columns:220px minmax(0,1fr)}.signals{display:none}}@media(max-width:760px){.shell{grid-template-rows:auto minmax(0,1fr)}.tape{min-height:52px;padding:10px 14px}.status{display:none}.desk{display:block}.book{display:flex;gap:7px;overflow-x:auto;padding:10px;border-right:0;border-bottom:1px solid var(--line)}.book .pane-head{display:none}.book-hint{display:block;flex:0 0 auto;align-self:center;padding:0 4px;color:var(--quiet);font-size:10px;white-space:nowrap}.position{flex:0 0 auto;width:auto;min-width:106px;padding:9px;border:1px solid var(--soft)}.position-meta span:last-child{display:none}.stage{padding:21px 15px 32px}.stage-header{display:block}.stage-header .button{margin-top:14px}.summary-grid,.layout-grid,.detail-grid{grid-template-columns:1fr 1fr}.summary-card.primary{grid-column:span 2}.mark-form{grid-template-columns:1fr}.ticket-values{grid-template-columns:1fr 1fr}}
@media(max-width:760px){.shell,.desk,.book{width:100%;min-width:0}.tape{min-width:0}.book{max-width:100%}.layout-grid{grid-template-columns:minmax(0,1fr)}.stack,.stress{min-width:0}}
`;
}

export function renderDeskApp(): string {
  return `
const app=document.getElementById('app');let state=null;let selectedTicker=null;let view='mirror';
const el=(tag,cls,text)=>{const node=document.createElement(tag);if(cls)node.className=cls;if(text!==undefined)node.textContent=String(text);return node};
const clear=node=>{while(node.firstChild)node.removeChild(node.firstChild)};
const percent=value=>value===null||value===undefined?'—':Number(value).toFixed(1)+'%';
const money=(amount,currency)=>new Intl.NumberFormat(undefined,{style:'currency',currency:currency||'USD',maximumFractionDigits:2}).format(Number(amount||0));
const dateTime=value=>value?new Date(value).toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'}):'not recorded';
const signed=(amount,currency)=>amount===null||amount===undefined?'—':(Number(amount)>=0?'+':'−')+money(Math.abs(Number(amount)),currency);
function toast(message,bad){document.querySelector('.toast')?.remove();const node=el('div','toast'+(bad?' bad':''),message);node.setAttribute('role',bad?'alert':'status');node.setAttribute('aria-live',bad?'assertive':'polite');document.body.append(node);setTimeout(()=>node.remove(),4200)}
function button(label,cls,onClick){const node=el('button',cls||'button',label);node.type='button';node.onclick=onClick;return node}
function field(label,name,value,type){const container=el('label','field',label);const input=el('input');input.name=name;input.type=type||'text';input.value=value||'';if(type==='number')input.step='any';container.append(input);return container}
function lineChart(values){if(!values||values.length<2)return el('div','empty-chart','No performance line has been invented. Record a daily mirror after reviewing the marks.');const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('viewBox','0 0 440 116');svg.setAttribute('class','line-chart');[22,58,94].forEach(y=>{const line=document.createElementNS(svg.namespaceURI,'line');line.setAttribute('x1','0');line.setAttribute('x2','440');line.setAttribute('y1',y);line.setAttribute('y2',y);svg.append(line)});const numbers=values.map(value=>Number(value.value));const low=Math.min(...numbers);const span=Math.max(...numbers)-low||1;const points=numbers.map((value,index)=>(index*440/(numbers.length-1)).toFixed(1)+','+(96-((value-low)/span)*76).toFixed(1)).join(' ');const polyline=document.createElementNS(svg.namespaceURI,'polyline');polyline.setAttribute('points',points);svg.append(polyline);return svg}
async function recordSnapshot(){try{const response=await fetch('/api/mirror/snapshot',{method:'POST'});const body=await response.json();if(!response.ok)throw new Error(body.error||'Could not record the daily mirror');toast('Daily mirror recorded');await load()}catch(error){toast(error.message,true)}}
async function mark(event,position){event.preventDefault();const form=event.currentTarget;const payload={ticker:position.ticker,price:Number(form.price.value),source:form.source.value.trim()||'Desk'};if(position.currency!==state.portfolio.baseCurrency)payload.fxRateToBase=Number(form.fx.value);try{const response=await fetch('/api/portfolio/mark',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error||'Could not save the mark');toast(position.ticker+' mark recorded');await load()}catch(error){toast(error.message,true)}}
async function decide(id,type,args){try{const response=await fetch('/api/decision/'+encodeURIComponent(id),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(args?{type,args}:{type})});const body=await response.json();if(!response.ok)throw new Error(body.error||'Could not submit decision');toast('Decision returned to the waiting agent');await load()}catch(error){toast(error.message,true)}}
function attentionCard(){const card=el('article','card card-body');card.append(el('h2','','Current constraints'));const list=el('div','attention-list');if(state.attention.length)state.attention.forEach(item=>list.append(el('div','attention-item '+item.level,item.message)));else list.append(el('div','attention-item clear','No concentration, valuation, or freshness constraint is currently raised.'));card.append(list);return card}
function allocationCard(){const card=el('article','card card-body');card.append(el('h2','','Capital at work'),el('p','card-note','Weights are based on the marked base-currency value shown here.'));const list=el('div','allocation-list');state.portfolio.positions.forEach(position=>{const row=el('div','allocation-row');row.append(el('span','ticker',position.ticker));const track=el('div','track');const fill=el('div','fill');fill.style.width=Math.min(Number(position.weight||0),100)+'%';track.append(fill);row.append(track,el('span','alloc-value',percent(position.weight)));list.append(row)});card.append(list);return card}
function stressCard(){const card=el('article','card card-body');card.append(el('h2','','Directional stress'),el('p','card-note','Only modeled holdings are included. Coverage is shown so an estimate never reads as whole-portfolio truth.'));const list=el('div','stress-grid');state.stress.forEach(item=>{const row=el('div','stress');const copy=el('div','');const coverage=item.coveragePct===100?'All marked capital modeled.':percent(item.coveragePct)+' modeled'+(item.unmodeledTickers.length?' · excludes '+item.unmodeledTickers.join(', '):'');copy.append(el('strong','',item.description),el('span','',coverage));const result=item.coveragePct===0?'Unmodeled':signed(item.totalImpact,state.portfolio.baseCurrency)+' · '+percent(item.totalImpactPct);row.append(copy,el('div','stress-impact',result));list.append(row)});card.append(list);return card}
function mirrorCard(){const card=el('article','card card-body');const head=el('div','mirror-head');head.append(el('h2','','Mirror'),el('span','count',state.mirror.equity.length+' daily marks'));card.append(head,lineChart(state.mirror.equity));const footer=el('div','mirror-footer');const drawdown=state.mirror.drawdown;footer.append(el('span','',drawdown.peak===null?'No high-water mark yet.':'Drawdown '+percent(drawdown.drawdownPct)+' from '+money(drawdown.peak,state.portfolio.baseCurrency)+'.'),button(state.mirror.equity.length?'Re-record today':'Record daily mirror','button ghost',recordSnapshot));card.append(footer);return card}
function mirrorStage(stage){const header=el('div','stage-header');const copy=el('div','');copy.append(el('div','eyebrow','Portfolio posture'),el('h1','','See what must be defended before looking for the next idea.'),el('p','subhead','This Desk does not guess at live values. It uses your dated marks, then makes concentration, freshness, and directional stress visible in one place.'));header.append(copy);stage.append(header);const summary=el('section','summary-grid');const total=el('article','card summary-card primary');total.append(el('div','eyebrow','Marked portfolio value'),el('div','value',money(state.portfolio.totalValueBase,state.portfolio.baseCurrency)),el('div',state.portfolio.fullyMarked?'good':'warning',state.portfolio.fullyMarked?'Every holding has an explicit price and conversion.':'Valuation contains a visible fallback.'));const singleLimit=state.policy.concentration.singlePositionPct;const topThreeLimit=state.policy.concentration.topThreePct;const top=el('article','card summary-card');top.append(el('div','eyebrow','Largest position'),el('div','metric-value '+(Number(state.portfolio.concentration.top1.weight)>singleLimit?'warning':''),percent(state.portfolio.concentration.top1.weight)),el('div','metric-label',state.portfolio.concentration.top1.ticker||'—'));const topThree=el('article','card summary-card');topThree.append(el('div','eyebrow','Top three'),el('div','metric-value '+(Number(state.portfolio.concentration.top3.weight)>topThreeLimit?'warning':''),percent(state.portfolio.concentration.top3.weight)),el('div','metric-label',state.portfolio.concentration.top3.tickers.join(' · ')||'—'));const fresh=el('article','card summary-card');const age=state.marks.oldestAgeDays;fresh.append(el('div','eyebrow','Mark freshness'),el('div','metric-value '+(age!==null&&age>state.marks.reviewThresholdDays?'warning':'good'),age===null?'—':age===0?'Today':age+'d'),el('div','metric-label',dateTime(state.marks.oldestMarkedAt)));summary.append(total,top,topThree,fresh);stage.append(summary);const grid=el('section','layout-grid');const left=el('div','stack');left.append(attentionCard(),allocationCard());const right=el('div','stack');right.append(stressCard(),mirrorCard());grid.append(left,right);stage.append(grid)}
function positionStage(stage,position){const header=el('div','stage-header');const copy=el('div','');copy.append(el('div','eyebrow','Position inspector'),el('h1','',position.ticker+' · '+percent(position.weight)+' of marked capital'),el('p','subhead',position.priceSource==='mark'?'The current view is tied to an explicit mark. Review its source and date before treating the numbers as current.':'This position is still using historical cost as a fallback. Record an explicit mark before relying on the risk view.'));header.append(copy,button('Back to posture','button ghost',()=>{view='mirror';render()}));stage.append(header);const details=el('section','detail-grid');[['Current price',money(position.price,position.currency),position.priceSource==='mark'?'explicit mark':'cost fallback'],['Market value',money(position.nativeValue,position.currency),money(position.valueBase,state.portfolio.baseCurrency)+' base'],['Unrealized P/L',signed(position.unrealizedPLNative,position.currency),percent(position.unrealizedPLPct)],['Portfolio weight',percent(position.weight),'rank '+(state.portfolio.positions.findIndex(item=>item.ticker===position.ticker)+1)]].forEach((item,index)=>{const stat=el('article','detail-stat');stat.append(el('span','',item[0]),el('b',index===2?(Number(position.unrealizedPLNative)<0?'bad':'good'):'',item[1]),el('span','',item[2]));details.append(stat)});stage.append(details);const pricing=el('article','card card-body detail-card');const pricingHead=el('div','detail-header');pricingHead.append(el('h2','','Pricing record'),el('span',position.priceSource==='mark'?'good':'warning',position.priceSource==='mark'?'explicit':'fallback'));pricing.append(pricingHead,el('p','source-line',position.priceSource==='mark'?(position.markSource||'Manual source')+' · observed '+dateTime(position.markedAt):'No explicit market mark has been recorded.'));const form=el('form','mark-form');form.append(field(position.currency+' price','price','','number'),field('Source','source',position.markSource||'Desk','text'));if(position.currency!==state.portfolio.baseCurrency)form.append(field(state.portfolio.baseCurrency+' per '+position.currency,'fx','','number'));const save=button('Record mark','button');save.type='submit';form.append(save);form.onsubmit=event=>mark(event,position);pricing.append(form);stage.append(pricing);const exposure=el('article','card card-body detail-card');exposure.append(el('h2','','Stress exposure'),el('p','card-note','Estimated contribution is shown only when this holding has an explicit or documented proxy.'));const list=el('div','stress-grid');state.stress.forEach(scenario=>{const impact=scenario.positions.find(item=>item.ticker===position.ticker);const row=el('div','stress');const detail=el('div','');const model=impact&&impact.modeled?impact.scenarioFactor+' · '+impact.factorSource+' assumption · '+percent(impact.impactPct):impact?.unmodeledReason||'No modeled exposure';detail.append(el('strong','',scenario.description),el('span','',model));row.append(detail,el('div','stress-impact',impact?signed(impact.impactDollars,state.portfolio.baseCurrency):'—'));list.append(row)});exposure.append(list);stage.append(exposure)}
function renderDecision(){const pending=state.pendingDecisions[0];if(!pending)return;const overlay=el('section','decision');const card=el('div','decision-card');card.append(el('div','eyebrow','Agent decision request'),el('h2','',pending.action_request?.action||'Decision'),el('p','',pending.description));const ticket=pending.action_request?.args;let ticketArgs=ticket&&typeof ticket==='object'?{...ticket}:undefined;if(ticketArgs){const values=el('div','ticket-values');['ticker','direction','entry','stop','shares','timeHorizon'].forEach(key=>{if(ticket[key]===undefined)return;const item=el('div','ticket-value');item.append(el('span','',key),el('b','',ticket[key]));values.append(item)});card.append(values);if(typeof ticket.shares==='number'&&typeof ticket.stop==='number'){const controls=el('div','ticket-controls');const shares=field('Shares','shares',String(ticket.shares),'number');const stop=field('Stop','stop',String(ticket.stop),'number');controls.append(shares,stop);const risk=el('p','ticket-risk');const updateRisk=()=>{const shareInput=shares.querySelector('input');const stopInput=stop.querySelector('input');const fx=Number(ticket.fxRateToBase||1);const dollars=Math.abs(Number(ticket.entry||0)*fx-Number(stopInput.value)*fx)*Number(shareInput.value||0);risk.textContent='Current ticket stop risk: '+money(dollars,ticket.baseCurrency||state.portfolio.baseCurrency)+' · '+percent(dollars/(state.portfolio.totalValueBase||1)*100)+'. Edited tickets must be re-sized by the agent.';ticketArgs={...ticket,shares:Number(shareInput.value),stop:Number(stopInput.value)}};shares.querySelector('input').oninput=updateRisk;stop.querySelector('input').oninput=updateRisk;updateRisk();card.append(controls,risk)}}let responseField;if(pending.config?.allow_respond){responseField=el('label','field','Response');const textarea=el('textarea');textarea.name='response';textarea.placeholder='Return a short condition or instruction to the agent';responseField.append(textarea);card.append(responseField)}const actions=el('div','decision-actions');if(pending.config?.allow_ignore!==false)actions.append(button('Ignore','button ghost',()=>decide(pending.requestId,'ignore')));if(pending.config?.allow_respond)actions.append(button('Respond','button ghost',()=>decide(pending.requestId,'respond',{...(ticketArgs||{}),response:responseField.querySelector('textarea').value})));if(pending.config?.allow_edit)actions.append(button('Return edits','button ghost',()=>decide(pending.requestId,'edit',ticketArgs)));if(pending.config?.allow_accept!==false)actions.append(button('Accept','button',()=>decide(pending.requestId,'accept',ticketArgs)));card.append(actions);overlay.append(card);app.append(overlay)}
function renderSignals(){const signals=el('aside','signals');const head=el('div','pane-head');head.append(el('h2','','Signals'),el('span','count',state.attention.length?state.attention.length+' attention':'clear'));signals.append(head);const integrity=el('section','integrity');integrity.append(el('strong','',state.portfolio.fullyMarked?'Valuation is explicit':'Valuation needs attention'),el('p','',state.portfolio.fullyMarked?'Every holding has a stated price source and required currency conversion.':'Risk views name any historical-cost fallback instead of hiding it.'));signals.append(integrity,el('div','section-rule'));const attentionHead=el('div','pane-head');attentionHead.append(el('h2','','Attention'),el('span','count',state.attention.length?state.attention.length+' open':'clear'));signals.append(attentionHead);if(state.attention.length)state.attention.forEach(item=>signals.append(el('div','attention-item '+item.level,item.message)));else signals.append(el('p','empty','No concentration, valuation, freshness, or model-coverage constraint is currently raised.'));const next=!state.readiness.hasThesis?'Record one falsifiable thesis before a new idea becomes a trade.':!state.readiness.hasShadowPlan?'Turn the next accepted thesis into a shadow plan, so execution can be measured.':!state.readiness.hasDailyMirror?'After reviewing marks, record a daily mirror to start a truthful curve.':null;if(next){signals.append(el('div','section-rule'));const readiness=el('article','signal');readiness.append(el('h3','','Start the loop'),el('p','',next));signals.append(readiness)}signals.append(el('div','section-rule'));const watchHead=el('div','pane-head');watchHead.append(el('h2','','Watchlist'),el('span','count',state.watchlist.length+' items'));signals.append(watchHead);if(state.watchlist.length)state.watchlist.slice(0,8).forEach(item=>{const signal=el('article','signal');signal.append(el('h3','',item.ticker||'WATCH'),el('p','',item.reason||'No note recorded'));signals.append(signal)});else signals.append(el('p','empty','No watchlist items. Add only ideas worth revisiting.'));return signals}
function render(){if(!state)return;const portfolio=state.portfolio;if(!selectedTicker&&portfolio.positions[0])selectedTicker=portfolio.positions[0].ticker;clear(app);const shell=el('div','shell');const tape=el('header','tape');const brand=el('div','brand');brand.append(el('span','wordmark','finstack'),el('span','section-name','Desk / '+(view==='mirror'?'Posture':'Position')));const status=el('div','status');status.append(el('span','dot'),el('span','','local · '+dateTime(state.generatedAt)));tape.append(brand,status);shell.append(tape);const desk=el('div','desk');const book=el('aside','book');const bookHead=el('div','pane-head');bookHead.append(el('h2','','Book'),el('span','count',portfolio.positions.length+' positions'));book.append(bookHead,el('span','book-hint','Swipe to inspect all positions'));portfolio.positions.forEach(position=>{const active=view==='position'&&position.ticker===selectedTicker;const item=el('button','position');item.type='button';item.setAttribute('aria-pressed',active?'true':'false');const row=el('div','position-row');row.append(el('span','ticker',position.ticker),el('span','weight',percent(position.weight)));const meta=el('div','position-meta');meta.append(el('span','',position.shares+' · '+position.currency),el('span','',money(position.nativeValue,position.currency)));item.append(row,meta);item.onclick=()=>{selectedTicker=position.ticker;view='position';render()};book.append(item)});desk.append(book);const stage=el('section','stage');const selected=portfolio.positions.find(position=>position.ticker===selectedTicker);if(view==='position'&&selected)positionStage(stage,selected);else mirrorStage(stage);desk.append(stage,renderSignals());shell.append(desk);app.append(shell);renderDecision()}
async function load(){try{const response=await fetch('/api/state');if(!response.ok)throw new Error('Desk state unavailable');state=await response.json();render()}catch(error){clear(app);app.append(el('div','loading',error.message))}}
const events=new EventSource('/events');events.addEventListener('state',event=>{state=JSON.parse(event.data);render()});load();
`;
}
