{
  "$schema": "https://modelcontextprotocol.io/schemas/hooks.schema.json",
  "hooks": {
    "onInstall": {
      "description": "Initialize finstack: verify Bun runtime and compile engine binary",
      "steps": [
        {
          "name": "check-bun",
          "command": "bash",
          "args": ["-c", "command -v bun >/dev/null 2>&1 || (echo '⏳ Installing Bun...' && curl -fsSL https://bun.sh/install | bash)"]
        },
        {
          "name": "build-engine",
          "command": "bash",
          "args": ["-c", "cd engine && echo '⏳ Building finstack engine...' && bun install --frozen-lockfile && bun run build"]
        }
      ]
    },
    "onUpgrade": {
      "description": "Rebuild engine if source changed since last version",
      "command": "bash",
      "args": ["-c", "cd engine && bun run build"]
    }
  }
}
